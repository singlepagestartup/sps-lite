{
  "framework": "nextjs",
  "installCommand": "npm ci --legacy-peer-deps",
  "buildCommand": "npx nx run host:db:migrate && npx nx run host:db:seed && npm run host:build",
  "outputDirectory": "apps/host/.next"
}
