{
  "framework": "nextjs",
  "installCommand": "bun install",
  "buildCommand": "nx run frontend:theme && bun run frontend:build && nx run frontend:db:migrate",
  "outputDirectory": "apps/frontend/.next"
}
