{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npm run frontend:build && nx run frontend:theme && nx run frontend:db:prepare",
  "outputDirectory": "apps/frontend/.next"
}
