{
  "framework": "nextjs",
  "installCommand": "npm ci",
  "buildCommand": "npx nx run frontend:db:migrate && npx nx run frontend:db:seed && npm run frontend:build",
  "outputDirectory": "apps/frontend/.next"
}
