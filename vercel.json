{
  "framework": "nextjs",
  "installCommand": "npm ci",
  "buildCommand": "npx nx run frontend:theme && npx nx run frontend:db:migrate && npx nx run frontend:db:seed && bun run frontend:build",
  "outputDirectory": "apps/frontend/.next"
}
