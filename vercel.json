{
  "framework": "nextjs",
  "installCommand": "bun i",
  "buildCommand": "bun run frontend:theme && bun run frontend:db:migrate && bun run frontend:db:seed && bun run frontend:build",
  "outputDirectory": "apps/frontend/.next"
}
