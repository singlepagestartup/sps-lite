{
  "framework": "nextjs",
  "installCommand": "npm ci",
  "buildCommand": "nx run frontend:theme && npm run frontend:build && nx run frontend:db:migrate && nx run frontend:db:seed",
  "outputDirectory": "apps/frontend/.next"
}
