import { queryBuilder as populate } from "./populate";
import { queryBuilder as filters } from "./filters";
import { queryBuilder as orderBy } from "./order-by";

export const queryBuilder = {
  populate,
  filters,
  orderBy,
};
