import { relations } from "drizzle-orm";
import { Table } from "@sps/sps-third-parties/models/telegram-message/backend/schema/table";

export const Relations = relations(Table, (helpers) => {
  return {};
});
