{
  "name": "@sps/sps-billing",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/modules/sps-billing",
  "projectType": "library",
  "tags": [],
  "targets": {
    "lint": {},
    "tsc:build": {},
    "repository-generate": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          {
            "command": "nx run @sps/sps-billing:models:widget:repository-generate"
          }
        ]
      }
    },
    "repository-migrate": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          {
            "command": "nx run @sps/sps-billing:models:widget:repository-migrate"
          }
        ]
      }
    },
    "models:widget:repository-generate": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "cwd": "libs/modules/sps-billing/models/widget/backend/repository/database",
        "commands": [
          {
            "command": "drizzle-kit up --config=./src/lib/config.ts"
          },
          {
            "command": "drizzle-kit generate --config=./src/lib/config.ts"
          }
        ]
      }
    },
    "models:widget:repository-migrate": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "envFile": "apps/host/.env.local",
        "cwd": "libs/modules/sps-billing/models/widget/backend/repository/database",
        "commands": [
          {
            "command": "bun run ./src/lib/migrate.ts"
          }
        ]
      }
    }
  }
}
