import { relations } from "drizzle-orm";
import { Table } from "@sps/sps-notification/models/notification/backend/schema/table";

export const Relations = relations(Table, (helpers) => {
  return {};
});
