import { spsCrmFormRequestContracts } from "./sps-crm-form-request-contracts";

describe("spsCrmFormRequestContracts", () => {
  it("should work", () => {
    expect(spsCrmFormRequestContracts()).toEqual(
      "sps-crm-form-request-contracts",
    );
  });
});
