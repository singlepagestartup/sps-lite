import { Default } from "./Default";
// import { Locale } from "./Locale";

export const variants = {
  text: Default,
  primary: Default,
  secondary: Default,
  // locale: Locale,
  destructive: Default,
  outline: Default,
  ghost: Default,
  link: Default,
};
