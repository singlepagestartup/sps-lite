import { Default } from "./Default";

export const variants = {
  text: Default,
  primary: Default,
  secondary: Default,
  destructive: Default,
  outline: Default,
  ghost: Default,
  link: Default,
};
