import { variants as spsLiteVariants } from "./singlepage/variants";
import { variants as startupVariants } from "./startup/variants";

export const variants = {
  ...spsLiteVariants,
  ...startupVariants,
};
