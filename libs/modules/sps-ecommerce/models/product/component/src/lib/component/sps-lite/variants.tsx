import { IdFromUrl } from "./IdFromUrl";
import { Default } from "./Default";
import { InCart } from "./InCart";
import { List } from "./List";

export const variants = {
  "id-from-url": IdFromUrl,
  default: Default,
  "in-cart": InCart,
  list: List,
};
