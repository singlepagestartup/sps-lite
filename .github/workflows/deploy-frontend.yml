name: Create prerelease version deployment

on:
  pull_request:
    types: [closed]
    branches:
      - deploy-frontend

jobs:
  update-frontend:
    uses: ./.github/workflows/frontend.yml
    secrets: inherit
    with:
      SLEEP_TIME: 0
      TAG: ${{ github.sha }}
      ENVIRONMENT: "staging"
