{
  "$schema": "https://json-schema.org/schema",
  "$id": "SpsDeleteTypeScriptLibrary",
  "title": "",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The root component for creating component.",
      "x-prompt": "Select root component?",
      "$default": {
        "$source": "project"
      },
      "x-dropdown": "projects"
    }
  },
  "required": ["project"]
}
