const title = `SPS Store`;
const description = `Single Page Store Boilerplate`;
const fullDescription = `Single Page Store Boilerplate`;
const url = `singlepagestore.ru`;
const image = `/assets/images/seo.jpg`;

export const SEO = {
  index: {
    title: `${title} | ${description}`,
    description: fullDescription,
    url: url,
    image: `https://${url}${image}`,
    ogTitle: `${title}`,
    ogDescription: fullDescription,
    ogImage: `https://${url}${image}`,
    twitterCard: `summary_large_image`,
    twitterTitle: `${description} | ${title}`,
    twitterDescription: fullDescription,
    twitterImage: `https://${url}${image}`,
  },
};
